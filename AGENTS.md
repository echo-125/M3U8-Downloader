# Cat Catch Assistant 项目协作规范

## 语言与沟通

- 与用户沟通、代码注释解释、提交说明和文档全部使用中文。
- 错误提示、日志和 GUI 文案默认使用中文。
- 代码标识符、依赖名和技术术语可保留英文。

## 开发环境

- 操作系统：Windows 10 及以上。
- Rust 版本：stable。
- 默认终端：PowerShell 7。
- 目标平台：Windows x64。
- GUI 框架：`egui`。

## 项目原则

- 本项目是个人项目，不需要组织化协作流程。
- 优先保持代码清晰、模块边界明确、实现简单可靠。
- 核心下载逻辑不得依赖 GUI。
- GUI 只通过事件、状态快照和任务句柄与核心交互。
- 所有网络请求必须设置超时。
- 所有用户输入必须校验，错误信息不得泄露敏感数据。
- 禁止硬编码密钥、密码和账号。

## 目录职责

```text
src/app/       # egui 界面、主题、布局、托盘
src/core/      # M3U8 解析、下载、解密、合并、任务调度
src/config/    # 配置结构与持久化
src/logging/   # GUI 日志与文件日志
src/ffmpeg/    # ffmpeg 检测与 remux
assets/        # 图标与静态资源
```

新增文件时优先放入已有职责目录；确实需要新目录时，先在对话中说明理由。

## 开发流程

1. 开发前先阅读 `DEVELOPMENT_PLAN.md`。
2. 按里程碑推进，不跳阶段实现。
3. 每个阶段完成后进行人工验收。
4. 修改完成后进行代码审查。
5. 提交前确认没有无关文件变更。

## 代码风格

- 使用 Rust 标准格式化风格，提交前执行 `cargo fmt`。
- 使用 `cargo clippy` 检查明显问题。
- 优先使用强类型结构表示状态、事件和配置。
- 避免过度抽象，不提前设计未使用接口。
- 不添加与功能无关的注释。
- 不引入无必要依赖。
- 公共 API 保持最小暴露范围。

## 错误处理

- 底层模块错误使用 `thiserror` 定义明确类型。
- GUI 层将错误转换为用户可理解的中文提示。
- 不允许使用空 `catch` 或忽略关键错误。
- 下载、解析、解密、合并等失败必须记录日志。

## 并发与异步

- 使用 `tokio` 管理异步任务。
- 不得在 GUI 线程执行阻塞 IO。
- 任务取消必须及时传播。
- 分片下载并发必须受配置限制。
- 多任务调度必须同时考虑任务并发和全局连接压力。

## 文件与路径

- 配置和日志优先放在程序同目录。
- 程序目录不可写时，降级到用户目录。
- 临时文件使用统一任务目录管理。
- 下载成功后按配置清理临时文件。
- 删除文件前必须确认路径属于该任务的临时目录。

## GUI 要求

- 界面语言仅中文。
- 亮色 / 暗色主题必须可切换并持久化。
- 关闭窗口默认最小化到系统托盘。
- 任务列表保持现有列设计。
- 不实现键盘快捷键。
- 高频进度事件必须在 GUI 层合并刷新。

## Git 规则

- 在用户明确要求前，禁止执行 `git add`、`git commit`、`git push`。
- 不主动创建分支。
- 不修改 Git 配置。

## 验证要求

- 当前阶段暂不要求编写自动化测试。
- 修改代码后至少执行 `cargo fmt`。
- 逻辑改动后执行 `cargo check`。
- GUI 改动后需要手动启动程序检查主流程。
- 发布前必须执行 `cargo build --release`。

## 发布要求

- 发布产物为 Windows x64 便携版单 exe。
- 不做代码签名。
- 不做自动更新。
- 不配置 CI/CD。
