# Cat Catch Assistant — Rust 完全重构开发计划

## 当前进度

- M1–M8 代码实现已完成，发布版已构建。
- 已通过 `cargo fmt`、`cargo test`、`cargo clippy --all-targets -- -D warnings` 和 `cargo build --release`。
- 真实网络下载、AES/fMP4 样例、托盘交互和 GUI 主流程仍需按第 11 节清单进行人工验收。

## 1. 项目定位

将现有 Python 版 M3U8 视频下载器完全重构为 Rust 桌面应用，目标是保持功能等价的同时降低内存占用、提升启动速度和运行稳定性。

本项目为个人项目，不设组织化协作流程，不引入代码签名、自动更新、CI/CD 和自动化测试要求。

## 2. 已确认需求

### 2.1 产品形态

- 仅保留 GUI 桌面版，不提供 CLI。
- GUI 框架使用 `egui`。
- 目标平台：Windows 10 及以上，x64。
- 发布形态：便携版单 exe。
- 不做代码签名。
- 不做自动更新。
- 仅支持中文界面。

### 2.2 功能范围

完全继承现有 Python GUI 的核心功能，但去除键盘快捷键：

- 单个任务添加
- 批量任务添加
- 手动合并 TS 分片
- 多任务并行下载
- 任务队列与自动调度
- 分片级断点续传
- 任务取消、重试、删除、清除
- 任务状态与进度展示
- 右键任务操作
- 双击任务快捷操作
- 自定义请求头
- 亮色 / 暗色主题
- 系统托盘
- 失败 Toast 提醒
- 设置面板
- GUI 日志面板
- 文件日志
- 代理支持
- 尾部加速

不包含：

- 键盘快捷键
- CLI
- 代码签名
- 自动更新
- CI/CD
- 自动化测试
- 多语言
- 限速
- 音频 / 字幕轨道选择
- 转码、压缩、分辨率调整
- 浏览器 Cookie 导入
- 下载内容安全扫描

### 2.3 M3U8 支持范围

继承现有版本支持的 M3U8 能力：

- VOD M3U8
- 主播放列表递归解析
- 最高带宽变体选择
- AES-128 CBC 解密
- 显式 IV
- 隐式 IV，基于 `MEDIA-SEQUENCE` 推导
- key rotation
- TS 分片
- fMP4 / CMAF 分片
- `EXT-X-MAP` 初始化段
- 初始化段 `BYTERANGE`
- 分片格式采样诊断

不包含：

- 直播流录制
- DRM
- SAMPLE-AES
- 非标准私有扩展的深度兼容

### 2.4 下载与任务策略

- 支持多任务并行下载。
- 支持配置最大并发任务数。
- 支持配置单任务下载并发数。
- 支持全局代理配置。
- 支持自定义请求头。
- 断点续传粒度为分片级。
- 429 自动重试，优先尊重 `Retry-After`。
- 5xx 自动重试。
- 网络错误和超时自动重试。
- 404 不自动重试。
- 403 不自动重试，提示用户检查防盗链或请求头。
- 保留尾部加速，支持配置阈值和并发提升倍数。

### 2.5 临时文件与合并策略

- 下载失败时默认保留临时文件。
- 下载成功后默认自动清理临时文件。
- 用户可选择保留临时文件用于排查问题。
- TS 分片：二进制拼接后调用 ffmpeg remux 为 MP4。
- fMP4 / CMAF：`EXT-X-MAP` 初始化段 + 分片直接拼接。
- ffmpeg 默认从 PATH 自动检测。
- 用户可在设置中手动指定 ffmpeg 路径。

### 2.6 配置与日志

- 设计全新配置文件，不迁移旧 `config.json`。
- 不迁移已有未完成任务。
- GUI 日志保留最近 500 条。
- 文件日志按天或大小滚动。

## 3. 技术方案

### 3.1 技术栈

| 领域       | 选型                             |
| -------- | ------------------------------ |
| 语言       | Rust stable                    |
| GUI      | `egui`                         |
| 异步运行时    | `tokio`                        |
| HTTP 客户端 | `reqwest`                      |
| AES 解密   | `aes` + `cbc`                  |
| 配置       | `serde` + `serde_json`         |
| 错误处理     | `thiserror`                    |
| 日志       | `tracing` + `tracing-appender` |
| 系统托盘     | `tray-icon`                    |
| 窗口管理     | `winit` / `eframe`             |
| 文件对话框    | `rfd`                          |
| 打包       | `cargo build --release`        |

### 3.2 架构原则

采用“核心逻辑与界面分离”的结构：

- 下载核心不直接依赖 GUI。
- GUI 通过任务句柄、事件通道和状态快照驱动核心。
- 下载任务使用异步状态机管理生命周期。
- 所有任务操作通过消息队列传递，避免 UI 线程阻塞。
- 配置、日志、下载核心、GUI 分别独立成模块。

## 4. 目标目录结构

```text
CatCatch/
├── Cargo.toml
├── DEVELOPMENT_PLAN.md
├── assets/
│   └── icon.ico
├── src/
│   ├── main.rs
│   ├── app/
│   │   ├── mod.rs
│   │   ├── state.rs
│   │   ├── theme.rs
│   │   ├── layout.rs
│   │   ├── widgets.rs
│   │   └── tray.rs
│   ├── core/
│   │   ├── mod.rs
│   │   ├── playlist.rs
│   │   ├── downloader.rs
│   │   ├── decrypt.rs
│   │   ├── merge.rs
│   │   ├── format.rs
│   │   ├── proxy.rs
│   │   ├── retry.rs
│   │   ├── tail.rs
│   │   ├── task.rs
│   │   ├── events.rs
│   │   └── error.rs
│   ├── config/
│   │   ├── mod.rs
│   │   ├── settings.rs
│   │   └── persistence.rs
│   ├── logging/
│   │   ├── mod.rs
│   │   └── rolling.rs
│   └── ffmpeg/
       ├── mod.rs
       ├── detect.rs
       └── remux.rs
```

## 5. 核心模块设计

### 5.1 播放列表解析

职责：

- 下载并解析 M3U8 文本。
- 识别主播放列表。
- 递归解析变体播放列表。
- 选择最高带宽流。
- 解析媒体分片 URL。
- 解析 `EXT-X-KEY`。
- 解析 `EXT-X-MAP`。
- 解析 `EXT-X-MEDIA-SEQUENCE`。
- 处理相对 URL 与 Base URL 拼接。

输出结构：

- 播放列表类型
- 分片列表
- 加密信息
- 初始化段信息
- 媒体序列号
- 总分片数

### 5.2 下载调度器

职责：

- 管理单个下载任务生命周期。
- 控制单任务并发下载数。
- 支持多任务队列。
- 支持取消和重试。
- 支持分片级断点续传。
- 支持尾部加速。
- 支持代理和自定义请求头。

任务状态：

- 等待中
- 下载中
- 取消中
- 已完成
- 已失败
- 已取消

### 5.3 加密模块

职责：

- 下载 AES-128 密钥。
- 支持 key rotation。
- 支持显式 IV。
- 支持基于 `MEDIA-SEQUENCE` 的隐式 IV。
- 使用 AES-128-CBC 解密。
- 解密后进行基本格式校验，避免密钥或 IV 错误时生成垃圾数据。

### 5.4 合并模块

职责：

- TS 分片二进制拼接。
- fMP4 初始化段与分片直接拼接。
- 调用 ffmpeg remux。
- 处理输出文件冲突。
- 处理临时文件清理策略。

### 5.5 格式诊断

职责：

- 采样识别 TS。
- 采样识别 fMP4。
- 识别 HTML 错误页。
- 识别 JSON 错误响应。
- 输出人类可读诊断信息。

### 5.6 事件系统

核心通过事件通道向 GUI 推送状态：

- 任务状态变化
- 分片完成
- 进度变化
- 速度变化
- 剩余时间变化
- 错误信息
- 日志信息
- 任务完成
- 任务取消

GUI 每帧只消费一次事件队列，避免高频进度事件造成界面卡顿。

## 6. GUI 设计

### 6.1 布局结构

界面从上到下分为：

1. 标题栏
2. 任务创建区
3. 任务列表区
4. 日志区
5. 状态栏

### 6.2 标题栏

- 应用名称与版本
- 主题切换按钮
- 设置按钮
- 最小化到托盘提示

### 6.3 任务创建区

使用标签页：

- 单个任务
- 批量添加
- 手动合并

单个任务字段：

- M3U8 链接
- 保存路径
- 文件名
- 线程数
- 自定义请求头 JSON

批量添加格式：

```text
链接|文件名|请求头JSON
```

手动合并字段：

- TS 文件夹
- 输出文件名
- 是否转换为 MP4
- 扫描按钮
- 开始合并按钮

### 6.4 任务列表

保持现有设计：

| 列       | 说明                                |
| ------- | --------------------------------- |
| 文件名     | 输出文件名                             |
| 状态      | 等待中 / 下载中 / 取消中 / 已完成 / 已失败 / 已取消 |
| 进度      | 百分比或已下载分片数                        |
| 速度 / 信息 | 下载速度、剩余时间或错误信息                    |

状态颜色：

- 等待中：灰色
- 下载中：蓝色
- 取消中：黄色
- 已完成：绿色
- 已失败：红色
- 已取消：橙色

工具栏按钮：

- 开始
- 全部开始
- 取消
- 重试
- 删除
- 清除

右键菜单：

- 开始
- 取消
- 编辑
- 重试
- 删除
- 复制链接
- 打开目录

交互：

- 等待中 / 已取消 / 已失败任务双击开始。
- 下载中任务双击取消。
- 不提供键盘快捷键。

### 6.5 设置面板

设置项：

- 默认下载路径
- 最大并发任务数
- 默认单任务线程数
- 自动清理临时文件
- 保留临时文件
- 尾部加速阈值
- 尾部加速倍数
- 代理类型
- 代理地址
- 代理端口
- 代理认证
- ffmpeg 自动检测
- ffmpeg 手动路径
- 日志文件滚动策略

### 6.6 主题

- 亮色主题
- 暗色主题
- 一键切换
- 主题状态持久化

### 6.7 系统托盘

- 关闭窗口时最小化到托盘。
- 托盘菜单包含“显示主窗口”和“退出程序”。
- 退出时如有下载中任务，弹窗确认。

## 7. 配置文件设计

配置文件位于程序同目录，使用 JSON 格式。

```json
{
  "download_path": "",
  "max_concurrent_downloads": 3,
  "max_workers": 16,
  "auto_cleanup": true,
  "keep_temp": false,
  "tail_threshold": 90,
  "tail_boost": 2,
  "proxy": {
    "enabled": false,
    "scheme": "http",
    "host": "",
    "port": 0,
    "username": "",
    "password": ""
  },
  "ffmpeg": {
    "auto_detect": true,
    "manual_path": ""
  },
  "logging": {
    "file_enabled": true,
    "max_gui_entries": 500,
    "rotation": "daily",
    "max_size_mb": 10
  },
  "appearance": {
    "theme": "light",
    "window_width": 960,
    "window_height": 720
  }
}
```

## 8. 开发里程碑

### M1：项目骨架

目标：

- 创建 Rust 项目。
- 接入 `egui`。
- 搭建主窗口、主题、基础布局。
- 建立模块目录。
- 接入基础日志。

验收：

- 程序可以启动。
- 亮色 / 暗色主题可切换。
- 空任务列表和日志区可显示。

### M2：配置与设置

目标：

- 实现配置文件读写。
- 实现设置面板。
- 实现代理配置。
- 实现 ffmpeg 自动检测与手动指定。

验收：

- 配置可持久化。
- ffmpeg 可检测并显示状态。
- 设置修改后立即生效。

### M3：M3U8 解析

目标：

- 实现播放列表下载。
- 实现主播放列表递归解析。
- 实现最高带宽选择。
- 实现分片提取。
- 实现 `EXT-X-KEY` 解析。
- 实现 `EXT-X-MAP` 解析。

验收：

- 输入 M3U8 后可正确解析分片数量。
- 可正确识别 AES-128 和 fMP4 初始化段。

### M4：下载核心

目标：

- 实现分片并发下载。
- 实现多任务调度。
- 实现断点续传。
- 实现取消、重试、失败处理。
- 实现尾部加速。
- 实现代理和自定义请求头。

验收：

- 多任务可并行下载。
- 失败任务可重试。
- 下载中任务可取消。
- 程序重启后可继续未完成分片。

### M5：AES-128 解密

目标：

- 实现密钥下载。
- 实现 AES-128-CBC 解密。
- 支持显式 IV。
- 支持隐式 IV。
- 支持 key rotation。
- 实现解密后格式校验。

验收：

- 加密 TS 可正确解密。
- key rotation 场景可正确切换密钥。

### M6：合并与 ffmpeg

目标：

- 实现 TS 二进制拼接。
- 实现 fMP4 init + 分片直接拼接。
- 实现 ffmpeg remux。
- 实现输出文件冲突处理。
- 实现临时文件清理策略。
- 实现手动合并功能。

验收：

- TS 输出 MP4。
- fMP4 输出可用视频。
- 成功任务可自动清理。
- 失败任务默认保留临时文件。

### M7：完整 GUI

目标：

- 完成任务列表。
- 完成右键菜单。
- 完成双击交互。
- 完成 Toast 提醒。
- 完成日志面板。
- 完成统计栏。
- 完成系统托盘。
- 完成窗口关闭行为。

验收：

- GUI 操作与任务状态实时同步。
- 关闭窗口最小化到托盘。
- 托盘可恢复和退出。
- 失败任务可弹 Toast。

### M8：打磨与发布

目标：

- 优化界面细节。
- 优化内存占用。
- 优化任务调度稳定性。
- 处理边界情况。
- 生成 Windows x64 便携版 exe。

验收：

- 便携版 exe 可直接运行。
- 主流程可完整下载、合并、输出 MP4。
- 无明显界面卡顿或任务状态错乱。

## 9. 实施顺序建议

建议按以下顺序推进：

1. 项目骨架
2. 配置系统
3. GUI 基础布局
4. M3U8 解析
5. 下载核心
6. AES 解密
7. 合并与 ffmpeg
8. 任务列表与交互
9. 托盘与主题
10. 日志与错误提示
11. 稳定性打磨
12. 便携版打包

## 10. 主要风险

| 风险                          | 应对                           |
| --------------------------- | ---------------------------- |
| `egui` 表格交互与现有 Treeview 有差异 | 先实现核心列表，再逐步补齐右键菜单和多选         |
| 系统托盘与 `egui` 事件循环集成复杂       | 优先保证最小化到托盘和退出，复杂交互后续增强       |
| fMP4 / CMAF 兼容性差异           | 保留格式诊断和 ffmpeg fallback 思路   |
| AES IV 推导错误                 | 显式区分显式 IV 和隐式 IV，并在解密后做格式校验  |
| 多任务并发导致网络拥塞                 | 同时限制任务级并发和全局并发               |
| 便携版路径权限问题                   | 配置与日志优先放在程序目录，遇到只读目录时降级到用户目录 |

## 11. 验收清单

### 基础功能

- [ ] 程序可启动
- [ ] 亮色 / 暗色主题可切换
- [ ] 设置可保存并恢复
- [ ] 日志面板可用
- [ ] 文件日志可生成

### 下载功能

- [ ] 普通 TS M3U8 可下载
- [ ] 主播放列表可递归解析
- [ ] AES-128 可解密
- [ ] key rotation 可用
- [ ] fMP4 / CMAF 可下载
- [ ] `EXT-X-MAP` 可下载
- [ ] 断点续传可用
- [ ] 429 / 5xx 可重试
- [ ] 404 不自动重试
- [ ] 403 不自动重试并提示
- [ ] 尾部加速可配置
- [ ] 代理可用
- [ ] 自定义请求头可用

### GUI 功能

- [ ] 单个任务可添加
- [ ] 批量任务可添加
- [ ] 多任务可并行下载
- [ ] 任务可取消
- [ ] 任务可重试
- [ ] 任务可删除
- [ ] 任务可清除
- [ ] 右键菜单可用
- [ ] 双击任务可用
- [ ] 状态颜色正确
- [ ] 进度实时刷新
- [ ] 速度和剩余时间显示
- [ ] 失败 Toast 可用
- [ ] 手动合并可用
- [ ] 关闭窗口最小化到托盘
- [ ] 托盘可恢复窗口
- [ ] 托盘可退出程序

### 输出功能

- [ ] TS 可 remux 为 MP4
- [ ] fMP4 可直接拼接输出
- [ ] ffmpeg 自动检测可用
- [ ] ffmpeg 手动路径可用
- [ ] 成功后临时文件可清理
- [ ] 失败后临时文件默认保留
